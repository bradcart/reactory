import styled from "styled-components";
import { flexbox, grid, space, layout, position, color } from "styled-system";

const Flex = styled.div`
  display: flex;
    ${flexbox}
    ${grid}
    ${space}
    ${layout}
    ${position}
    ${color}
`;

export default Flex;
